{
    "name": "demo-app",
    "version": "1.0.0",
    "scripts": {
    "start": "node server.js",
    "test": "node test.js",
    "build": "mkdir -p dist && cp -r public/* dist/"
    },
    "dependencies": {
    "express": "^4.18.2"
    }
}