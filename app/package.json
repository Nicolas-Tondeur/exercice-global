{
  "name": "mini-app-test",
  "version": "1.0.0",
  "description": "Mini-application pour déploiement",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Build successful' > build.log",
    "test": "echo 'Tests successful' && exit 0"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}